import { timeSinceSubmitted } from '../../../helpers/time-since-submitted';
import { module, test } from 'qunit';

module('Unit | Helper | time since submitted');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timeSinceSubmitted(42);
  assert.ok(result);
});
