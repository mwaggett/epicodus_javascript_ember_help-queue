import { currentQuestion } from '../../../helpers/current-question';
import { module, test } from 'qunit';

module('Unit | Helper | current question');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = currentQuestion(42);
  assert.ok(result);
});
